# Select component registeres mobile touch click behind content (temp fixed)

# In catalog page price filter input element doesn't get initial value

# In filter location list doesn't scroll on mobile
